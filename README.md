## RapidQuery

**RapidQuery is a dynamic SQL query builder built top of SeaQuery**, a Rust crate.
RapidQuery is a Python library, but leverages the Rust power to has high-performance;
You can construct SQL expressions and queries in Python, as fast as you can imagine.

RapidQuery is the core library of RapidORM library, which is comming soon ...
